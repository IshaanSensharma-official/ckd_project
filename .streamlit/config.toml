[server]
headless = true
port = 8501

[theme]
base = "light"
```

**2. Check your `requirements.txt`** — make sure it looks exactly like this (Streamlit Cloud is strict about versions):
```
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0
streamlit>=1.35.0
matplotlib>=3.7.0
seaborn>=0.12.0
```

**3. Verify your `.gitignore`** — make sure the model `.pkl` files are NOT ignored. Open `.gitignore` and confirm the models line is commented out (it should already be):
```
# models/*.pkl   ← this line must be commented out or absent